
let token=null; let watchId=null; const statusEl=document.getElementById('status');
function setStatus(t){ statusEl.textContent=t; }
async function login(){ const email=document.getElementById('email').value.trim(); const password=document.getElementById('pass').value; const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})}); const d=await r.json(); if(!r.ok){ document.getElementById('loginMsg').textContent=d.error||'Error'; return; } token=d.token; document.getElementById('loginMsg').textContent='OK ✔️'; document.getElementById('startBtn').disabled=false; }
async function sendLocation(pos){ const { latitude:lat, longitude:lng, speed, heading, accuracy } = pos.coords; const payload={lat,lng,speed,heading,accuracy}; const r=await fetch('/api/location',{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${token}`},body:JSON.stringify(payload)}); if(!r.ok){ setStatus('Error enviando ubicación'); }}
function start(){ if(!('geolocation' in navigator)){ setStatus('Geolocalización no soportada'); return;} setStatus('Rastreo iniciado…'); document.getElementById('startBtn').disabled=true; document.getElementById('stopBtn').disabled=false; watchId=navigator.geolocation.watchPosition(sendLocation,(err)=>{ setStatus('Error de GPS: '+err.message); },{ enableHighAccuracy:true, maximumAge:1000, timeout:10000 }); }
function stop(){ if(watchId!==null){ navigator.geolocation.clearWatch(watchId);} watchId=null; setStatus('Rastreo detenido'); document.getElementById('startBtn').disabled=false; document.getElementById('stopBtn').disabled=true; }
(document.getElementById('loginBtn').onclick=login); (document.getElementById('startBtn').onclick=start); (document.getElementById('stopBtn').onclick=stop);
